ENCODER_ENABLE = yes

# Per-key debounce: isolates each key's debounce window so a bouncy switch
# can't delay or drop other keypresses. Fixes both double-typing and missed presses.
DEBOUNCE_TYPE = sym_defer_pk
